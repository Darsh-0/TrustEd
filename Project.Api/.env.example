# The DAO's UniversityRegistry — the source of truth for accreditation.
# Take this from university-dao/deployments/<chainId>.json ("registry").
UNIVERSITY_REGISTRY_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0

# Node the DAO is deployed on.
RPC_URL=http://127.0.0.1:8545

PORT=5000
